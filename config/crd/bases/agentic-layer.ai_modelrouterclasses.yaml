---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: modelrouterclasses.agentic-layer.ai
spec:
  group: agentic-layer.ai
  names:
    kind: ModelRouterClass
    listKind: ModelRouterClassList
    plural: modelrouterclasses
    singular: modelrouterclass
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModelRouterClass is the Schema for the modelrouterclasses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelRouterClassSpec defines the desired state of ModelRouterClass.
            properties:
              controller:
                description: Controller is the name of the controller that should
                  handle this gateway class
                type: string
            required:
            - controller
            type: object
          status:
            description: ModelRouterClassStatus defines the observed state of ModelRouterClass.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
